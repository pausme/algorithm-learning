//
// Created by zx208 on 2023/5/8.
//
#include <bits/stdc++.h>
using namespace std;

int main(){
    // 操作电子表格
    int n;
    cin >> n;
    while (n--){
        string op;
        cin >> op;
        if(op == "EX"){
            int r1, c1, r2, c2;
            cin >> r1 >> c1 >> r2 >> c2;

        } else if(op == "DC"){

        } else if(op == "DR"){

        } else if(op == "IC"){

        } else if(op == "IR"){

        } else{

        }
    }
    //查询
    int q;
    cin >> q;
    while (q--){
        int r, c;
        cout <<
    }
    return 0;
}