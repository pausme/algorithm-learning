//
// Created by zx208 on 2023/7/19.
//
//http://poj.org/problem?id=3579
#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int a[N];

int main(){
    int num;
    while(scanf("%d", &num) && num){
        memset(a, 0, sizeof a);
        for (int i = 0; i < num; ++i) {

        }
    }
    return 0;
}