//
// Created by zx208 on 2023/8/20.
//
/*
 * 菲尔正在学习一种新的算法，这是他的算法课上没有教过的。
 * 然而，他不确定自己是否以正确的方式实施了它，所以如果你能实施它，以便他能够比较输出，他将非常感激。
 * 该算法从一个二进制字符串a和一个空字符串S开始。该算法由多个步骤组成。
 * 在每个步骤中，A和S的修改如下：
 * 如果A中的位数为奇数，则将A的中间位添加到S的末尾并从A中移除。
 * 如果A中位数为偶数，则比较A的两个中间位。
 * 较大的一个（在平局的情况下的任何人）被添加到S的末尾并从a中删除。
 * 如果在某个步骤之后字符串a变空，则算法终止。该算法的返回是由s表示的数字的十进制表示。
 * 如果A是1并且b是0，则位A大于位b。
 */