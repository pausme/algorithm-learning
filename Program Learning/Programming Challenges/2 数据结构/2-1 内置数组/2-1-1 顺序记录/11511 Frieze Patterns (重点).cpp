//
// Created by zx208 on 2023/8/31.
//
#include <bits/stdc++.h>
using namespace std;
const int N = 1e3 + 10;
int a[N];

int main(){
    int nums;
    while(cin >> nums, nums){
        memset(a, 0, sizeof a);
        int i, j;
        cin >> i >> j;
        for (int k = 0; k < nums; ++k) {
            cin >> a[i];
        }
    }
    return 0;
}